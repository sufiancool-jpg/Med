Laptop handoff backup created on 2026-03-25.

Contents:
- `plugin-zips/`: local plugin zip artifacts that existed only on the laptop working tree.
- `stash-codex-autostash-before-pull-2026-03-05.patch`: export of the remaining local stash from `stash@{0}`.

Notes:
- This backup branch is for preservation only. `main` remains the working branch to use on the new PC.
- Secrets and local runtime files such as `.env` and `.wp-local/` are not stored in GitHub and must be transferred separately.
